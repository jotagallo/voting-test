voting.vote:
  path: '/vote/{node}' 
  defaults: 
    _controller: '\Drupal\voting\Controller\VotesController::vote'
  requirements: 
    _permission: 'is logged in'
  options:
    parameters:
      node:
        type: entity:node